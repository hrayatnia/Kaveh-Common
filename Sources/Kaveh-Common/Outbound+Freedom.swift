import MemberwiseInit

/// Settings for Freedom outbound protocol.
@MemberwiseInit(.public)
@frozen public struct OutboundFreedomSettings: Codable {
    
  public init(from decoder: any Decoder) throws {
  }
  
}
